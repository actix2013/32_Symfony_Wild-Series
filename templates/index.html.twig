{% extends "base.html.twig" %}
{% block title %}
    Welcome!, you are on {{ website }}
{% endblock %}


{% block body %}
    <div class="mt-xl-5 p-xl-3 myBlockTextBase border border-white">
    <h1>Bienvenue sur Wild Series !</h1>


    <a href="{{ path('wild_show', {'slug':"paroduit-1"}) }}">Afficher la page 1</a><br>
    <a href="{{ path('wild_show', {'slug':5}) }}">Afficher la page 5</a><br>
    {# templates/index.html.twig #}
    <a href="{{ path('wild_show', { 'slug': "walking-dead" }) }}">
        Testing show() method from WildController with a real program s slug.
    </a>
    <br>
    <a href="{{ path('wild_show', { 'slug': "Penny-Dreadful" }) }}">
        Testing show() method from WildController with a real program s slug Penny-Dreadful.
    </a>
    <br>
    <a href="{{ path('wild_index') }}">
        Testing index() method from WildController to view all programs.
    </a>
    </div>

{% endblock %}
