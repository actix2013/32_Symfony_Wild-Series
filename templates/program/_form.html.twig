<div class="vich-image">
    {% if program.poster %}
        <img src="{{ vich_uploader_asset(program) }}" alt="NO">
    {% endif %}
</div>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="{{ btnDelete }}">{{ button_label|default('Save')
        }}</button>
{{ form_end(form) }}

